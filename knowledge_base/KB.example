# Kiro 知识库文档示例

> 这是知识库文件的示例模板。请将真实知识库内容保存为 `KB_1.md`、`KB_2.md` 等
> 以 `.md` 结尾的文件放入本目录（knowledge_base/）。
>
> 说明：
> - Agent 通过 `read_kb_file` 工具按需读取这些文件。
> - 工具描述会自动列出本目录下所有 .md 文件，并取每个文件的「首行」作为主题摘要，
>   因此建议每个 KB 文件的第一行写一个清晰的标题，概括该文件主题。
> - 真实 .md 文件不会提交到 Git（见 .gitignore），仅本示例 .example 模板保留。

## 示例：文件第一行作为摘要

例如某个 `KB_1.md` 的第一行可以是：

`# Kiro 使用前必读、常见基础问题（Credit、模型选择、CLI vs IDE）`

这样 Agent 在工具描述里就能看到：

`- KB_1.md: # Kiro 使用前必读、常见基础问题（Credit、模型选择、CLI vs IDE）`

从而判断何时读取该文件。

## 正文

在标题之后，按主题组织你的知识内容即可，使用标准 Markdown 语法。
